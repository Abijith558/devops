version: 0.2

phases:
  install:
    runtime-versions:
      python: 3.8

  build:
    commands:
      - python -m pip install --upgrade pip
      - pip install -r requirements.txt
      - pip install -r requirements-dev.txt
